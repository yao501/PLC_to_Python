---
description: 迁移 PLC 基础原语功能块（TON/TOF/TP/R_TRIG/F_TRIG/SR/RS 等）时使用的规则。当用户本轮任务是迁移这类基础原语时自动参考。
alwaysApply: false
---

# 基础原语功能块迁移规则

> 适用对象：`TON`、`TOF`、`TP`、`R_TRIG`、`F_TRIG`、`SR`、`RS`、`BLINK`、`CTU`、`CTD`、`FIFO`、`STACK` 等最底层、可复用的 PLC 原语功能块。

## 任务目标

迁移的是 PLC 基础原语功能块，而不是业务功能块。必须优先保证语义正确、周期行为正确、状态保持正确。

## 审核顺序（强制）

1. **先锁定原语语义，再写实现**：
   - 先依据当前确认材料（官方文档 / 截图 / ST 源码 / 已锁定事实）写出目标语义；
   - 未锁定前，不允许用工程直觉替代原语语义；
   - 若"原语语义"与"项目工程约定"不同，必须分开写，严禁混写。
2. **严格区分两类结论**：
   - **原语语义**：来自官方文档、截图、源码或已锁定事实；
   - **工程约定**：项目为了运行时一致性、可测试性、可终止性而做的选择；
   - 禁止把工程约定伪装成"官方已确认行为"。
3. **优先检查时序/状态边界，不只看常规路径**：
   - 停机/禁用时是否保持状态；
   - 重新启用时是续跑、复位还是其他行为；
   - 单拍 `dt_ms` 是否可能跨越多个相位 / 多个阈值；
   - 非等距扫描是否仍保持累计语义；
   - 长周期运行后是否漂移；
   - 退化输入下是否死循环或状态不收敛。
4. **凡存在多段状态推进，不得默认"每拍最多变化一次"**：
   - 必须先证明 runtime 已全局禁止大步长扫描；
   - 若不能证明，应优先实现为单拍内循环消费累计量，直到剩余量小于当前阈值。
5. **测试优先覆盖语义边界，而不是只覆盖 happy path**。
6. **RISKS.md 只登记真的未定项 / 延后项 / 保留行为**：
   - 不要把已确认的官方语义继续挂成风险；
   - 不要把已锁定的项目约定继续挂成"待确认"；
   - 已完成修复且已有测试锁定的事项，应改成 `resolved` 或从风险表删除。
7. **块内护栏必须标明性质**：
   - 必须说明它是防死循环 / 防数值爆炸 / 防非法状态扩散中的哪一类；
   - 并明确声明它**不是**参数合法化、不是业务规则兜底、不是上层 runtime 校验职责的替代。

## 实现要求

1. 将该功能块实现为 Python 类。
2. 接口优先采用 `step(dt_ms, ...)` 或 `update(dt_ms, ...)`。
3. 必须体现 PLC 扫描周期语义，而不是普通脚本语义。
4. 对于定时器/计时类功能块，必须基于 `dt_ms` 进行时间累积。
5. 对于触发器类功能块，必须依赖上一周期状态。
6. 对于锁存/计数/队列类功能块，必须正确保持跨周期状态。
7. 输出的代码要尽量简洁、清晰、可复用。
8. 不要引入无关框架。
9. 不要擅自改变该原语功能块的标准行为。
10. 如发现 IEC / CODESYS 语义存在不同实现习惯，请明确说明采用的假设。
11. 如需块内护栏，必须在 docstring / 测试 / `docs/RISKS.md` 三处统一写清：
    - 该护栏解决的技术问题是什么；
    - 它不替代哪一层的职责；
    - 它是否改变了原语语义。

## 输出结构

后续每次交付基础原语时，回复**优先固定**按以下 4 部分组织：

1. **锁定的原语语义**
2. **项目工程约定**
3. **时序 / 状态边界处理**
4. **测试如何覆盖这些边界**

如用户明确要求，或任务需要补充材料，再追加：

5. `step(dt_ms, ...)` 的最小调用示例
6. 风险点、假设和待确认项

## 推荐迁移顺序

优先按下面顺序迁移：`TON` → `TOF` → `TP` → `R_TRIG` → `F_TRIG` → `SR` → `RS`。

## 输入材料期望

希望用户尽量提供：原始 ST 代码或功能说明、输入输出定义、期望行为说明；如存在 IEC / CODESYS 具体实现差异说明更好。
