---
description: 构建 Python 侧主程序、统一扫描调度、Controller/MainProgram 和全局数据流时使用的规则。当用户本轮任务涉及主程序结构、CFC 调度、实例初始化与全局信号流时自动参考。
alwaysApply: false
---

# 主程序与扫描调度构建规则

> 适用对象：CFC 主程序、总控调度器、Controller / MainProgram。在基础原语块和业务功能块均已逐步迁移完成后使用。
> 
> 本规则必须与 `00a-runtime-contract` 联合解读：时间单位、扫描周期、`request / final_output` 分离、系统级输出门控等契约条款以 `00a` 为准，本规则不重述。

## 任务目标

基于已迁移完成的基础原语功能块和业务功能块，构建主程序 / Controller / MainProgram，并实现统一的 PLC 扫描调度模型。

## 主程序五步式（强制，见 `00a-runtime-contract` R6 第 5 条）

每个扫描周期严格按以下顺序执行，顺序不得颠倒：

1. **输入快照**：一次性采样所有物理/逻辑输入，本周期后续环节只读此快照。
2. **功能块推进**：按依赖顺序依次调用各 FB 实例的 `step(dt_ms, ...)`。
3. **request 生成**：业务逻辑仅输出命令请求 `request`，**不得直接写物理输出**。
4. **输出门控**：统一叠加 `system_ready / output_enable / safety_ok / interlock_ok`，按公式 `final_output = system_ready AND output_enable AND safety_ok AND interlock_ok AND request` 计算最终输出。
5. **一次性提交输出**：门控后的最终输出集中写出，不允许中途写半成品。

## 实现要求

1. 严格复用已存在的 Python 功能块实现，不要重写其内部逻辑。
2. 主程序统一入口签名为 `step(dt_ms: int, inputs) -> outputs`；`dt_ms` 单位见 `00a` R1。
3. 当前任务重点：
   - 主程序结构、实例初始化
   - 扫描周期调度、调用顺序
   - 中间变量与 `request` 传递
   - 输入快照 / 输出提交
   - 联锁与优先级关系
4. 所有功能块实例都要在主程序中被有序调度。
5. 如果某些顺序来自 CFC 连线，按连线和信号流重建；顺序不明确时基于合理假设给出最佳可行版本并明确说明。
6. 不要把主程序写成一次性脚本；要让整个系统具备"周期调用一次 → 所有状态推进一次"的运行模型。
7. 尽量保留原始变量名和结构语义。
8. **冷启动保护**：必须实现 `startup_inhibit_ms` 稳定窗口（见 `00a` 系统级输出安全第 4–5 条），并允许叠加 `io_ready / bus_ready / comm_ready / safety_ok`。
9. **安全默认值**：扫描异常 / 超时 / 主循环失败时，物理输出必须落到预定义安全默认值。
10. **Shadow mode / write disable**：首次接现场设备前默认启用，不允许上来就写物理输出。
11. **`PT_ms` 配置校验**：所有时间常量在初始化阶段统一走 `src.validation.check_pt_ms`，让不合规的配置在启动时一次性暴露。
12. 输出应尽量完整、可运行、可扩展。

## 输出结构

输出请严格按以下 10 部分组织：

1. 对主程序 / CFC 调度结构的理解
2. 整体调度设计说明（显式点出五步式）
3. Python 主程序代码
4. 功能块实例初始化代码（含 `check_pt_ms` 校验）
5. `step(dt_ms, inputs)` 的调用顺序说明
6. 中间 `request` 与数据流说明
7. 输入快照 / 输出门控 / 一次性提交逻辑说明
8. 冷启动、安全默认值、shadow mode 的落地方式
9. 风险点、假设和待确认项
10. 如有必要，给出最小运行示例

## 输入材料期望顺序

主程序涉及的功能块名单 → 已迁移完成的 Python 类名 → 变量表 → CFC 连线关系 → 局部或全局执行顺序说明 → 输入输出说明 → 联锁 / 安全约束 → 补充限制与待确认项。

## 使用前提

应当已具备：一批基础原语功能块、一批业务功能块 / 组合块、比较明确的主程序信号流。若当前仅为单个业务块或局部小逻辑，应先使用 `02-business-blocks` 规则。
