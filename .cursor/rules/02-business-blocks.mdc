---
description: 迁移业务功能块、设备基础块、局部组合逻辑（电机启停、阀门控制、报警延时、自动/手动切换、故障锁存等）时使用的规则。当用户本轮任务是迁移业务块或小范围组合块时自动参考。
alwaysApply: false
---

# 业务基础块与组合块迁移规则

> 适用对象：业务功能块、设备基础块、小范围组合逻辑。典型如电机启停保持、阀门控制、报警延时、自动/手动切换、故障锁存、局部设备单元逻辑。

## 任务目标

迁移业务基础块或局部组合块。必须在 Python 中保持其 PLC 周期行为、状态保持、输入输出更新顺序和逻辑语义一致。

## 实现要求

1. 严格复用已经迁移完成的基础原语功能块（`TON` / `TOF` / `TP` / `R_TRIG` / `F_TRIG` / `SR` / `RS`）。
2. 不要重复实现这些公共原语，除非用户明确要求重写。
3. 功能块优先映射为 Python 类。
4. 必须体现跨周期状态保持。
5. 必须正确处理：
   - 定时器行为
   - 边沿触发
   - 锁存 / 复位逻辑
   - 输入输出更新顺序
6. 迁移目标不是逐字翻译，而是在明确运行契约下保持行为一致。
7. 如该逻辑属于多个原语块组合，明确说明组合关系。
8. 不要擅自优化或改变业务行为。
9. 若发现潜在 bug、歧义、优先级不明确等问题，可以标记，但不要私自修改业务语义。
10. 输出尽量为可运行的 Python 代码，而不是伪代码。

## 输出结构

输出请严格按以下 8 部分组织：

1. 对该业务块 / 组合块的逻辑理解
2. 说明它依赖了哪些基础原语块
3. Python 实现
4. `step(dt, ...)` 的调用示例
5. 判断它属于：组合逻辑 / 时序逻辑 / 混合逻辑
6. 关键状态、输入输出、时序行为说明
7. 风险点、假设和待确认项
8. 如有必要，补一个最小测试示例

## 输入材料期望顺序

变量表 → FB / FUNCTION / PROGRAM 接口定义 → ST 代码 → 补充行为说明 → 该块依赖的已完成基础原语块名称。

## 适用范围

适合：单个业务功能块、由多个基础原语块构成的较小业务块、一个局部设备功能段、一个局部流程段的控制逻辑。

若已涉及全局调度、实例初始化、统一扫描入口、跨多个设备单元的大规模连接关系，应改用 `03-main-program` 规则。
