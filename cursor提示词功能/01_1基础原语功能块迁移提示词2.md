## 附录 A：给 Cursor 的当前精简共识版（适用于当前基础原语阶段）

> 用途：现在就可以给 Cursor。  
> 目标：约束当前 `TON / TOF / TP / R_TRIG / F_TRIG / SR / RS` 的修改方向，避免继续按旧契约扩散。  
> 适用阶段：基础原语功能块迁移、基础测试修正、时间语义修正。

```text
请在严格遵守“项目级总前置提示词”和“基础原语功能块迁移提示词”的前提下，额外遵守下面这组当前项目已确定的运行共识。当前任务仍然只处理基础原语块，不要擅自扩展到完整 runtime 或现场集成框架。

【一、项目目标】
1. 本项目不是普通 Python 代码翻译，而是 PLC 语义保持迁移。
2. 迁移重点是：扫描周期语义、跨周期状态保持、时间推进方式、边沿检测、锁存行为、统一调度兼容性。
3. 当前任务只处理基础原语块的语义定型，不要顺手扩展成完整运行时系统。

【二、基础原语块定位】
1. TON / TOF / TP / R_TRIG / F_TRIG / SR / RS 都应视为“有状态、可被调度的一拍推进内核”。
2. 它们必须支持跨周期状态保持。
3. 接口应保持 `step(...)` / `update(...)` 风格，以兼容后续主程序统一调度。
4. 不要把这些基础块写成一次性无状态函数。

【三、时间与扫描周期共识】
1. 生产语义按固定扫描周期理解。
2. 当前项目默认主程序周期：`cycle_ms = 500`。
3. 基础时间内部统一改为 `int ms`，不要继续使用 `float 秒` 作为内部主表示。
4. 定时器默认按 `ET_ms += dt_ms` 推进。
5. 禁止在基础块内部自行读取系统时钟。
6. `dt_ms` 由外部主程序/调度层传入，基础块只负责按本拍 `dt_ms` 推进状态。

【四、R1-R6 已确定处理原则】
1. R1：内部时间统一为 `int ms`。
2. R2：标准基础块本体不要默认添加 `RESET`，也不要把 `EN / ENO` 混进基础块本体接口；若后续源工程依赖 `EN / ENO`，在调度层处理。
3. R3：R_TRIG / F_TRIG 继续按“依赖上一周期状态”的模型实现；但冷启动防误动作不是基础块单独负责，而是后续由主程序输出门控统一保证。
4. R4：SR / RS 必须按电平锁存语义实现；SR 为置位优先，RS 为复位优先；如果业务需要“边沿触发后锁存”，应使用 `R_TRIG/F_TRIG + SR/RS` 组合，不要修改 SR/RS 本体语义。
5. R5：彻底禁止浮点时间累积；定时器内部统一使用整数时间累计。
6. R6：默认固定扫描周期模式，`dt_ms` 来源于主程序/任务配置周期，而不是 Python 实际运行耗时。

【五、冷启动与输出安全的分责边界】
1. 基础块尽量保持标准/常见 PLC 语义。
2. 冷启动防误动作、输出使能、统一门控，属于后续主程序 / runtime 层职责，不要求当前基础块自行承担全部安全责任。
3. 因此当前基础块实现中，应避免擅自引入过重的系统级安全逻辑。
4. 但代码结构必须兼容后续：
   - request / output 分离
   - system_ready / output_enable 门控
   - startup inhibit window

【六、当前修改任务的明确要求】
1. 现在就把 primitives 从 `float 秒` 改为 `int ms`。
2. 同步修改：
   - timers / triggers / latches 实现
   - step 接口
   - 测试
   - README / 示例
3. 保持接口清晰，不要混用 `float` 和 `int`。
4. 不要擅自扩展成完整 runtime 框架。
5. 代码输出时，必须清楚说明：
   - 当前块的 PLC 语义理解
   - 修改点
   - 与旧实现相比的变化
   - 与后续主程序调度的兼容方式
   - 风险点、假设、待确认项

【七、输出要求】
请输出：
1. 你对本轮精简共识的理解
2. 需要修改的具体点
3. 修改后的 Python 实现
4. 最小调用示例
5. 最小测试示例
6. 风险点、假设和待确认项

请始终以“PLC 语义保持迁移工程师”的身份输出，不要按普通 Python 脚本思维实现。
```