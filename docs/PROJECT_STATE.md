# 项目状态快照（PROJECT_STATE）

> **用途**：跨会话记忆载体。每个 AI 会话开始时**先读本文件**（配合 `CODEX_GUIDE.md` 长期工作方针）。
> **更新纪律**：仅当阶段、版本、完成项、阻塞项或下一步发生**实质变化**时更新，不做无意义编辑；只保留"当前状态 + 决策索引 + 下一步"，不写过程叙事；超过 150 行就该精简。
> **PR #35 独立审核恢复正式通过：2026-08-27**（`WP-20260827-152 CLOSED`）：新独立 Reviewer 对已推送 head `39a2969` 给出的两项 P2 已收口。所有命令在 exact-shell 零观察门禁后统一交冻结 `load_cfc_document` 验证输入语义，CFCEditResult 同样验证 before/after；重复 node id 与非法 carrier 直接构造反证已持久化。Claude 正式回审与新的 Codex 独立审核均通过，宿主 release V3 为 tests 2148/2148、prototype 68/68、root 2216/2216。PR #35 在本恢复提交推送前仍暂不合并；下面“四个提交/head=2711738”的 WP-150 文字属于恢复前快照，不再代表当前审计口径，最终提交序列由后续非自引用行政记录和 Git 实盘共同裁决。
> **Stage 2～4 Git/GitHub 基线已推送：2026-08-27**：分支 `codex/stage2-4-baseline-20260827` 已按职责形成四个提交：`5507ac1` 协作/备用流程、`ac6e773` Stage 2 CFC + Stage 3 ST 连续运行时基线、`0eff86f` Stage 4 文档模型与原子编辑命令、`2711738` 状态同步；[PR #35](https://github.com/yao501/PLC_to_Python/pull/35) 已创建并处于 OPEN / MERGEABLE。发布前独立实跑 ParserTests 27/27、tests 2145/2145、prototype 68/68、root 2213/2213。本段只升级 Git 轴为“已提交并推送、待 PR 审核/合并”；`main/origin/main` 仍为 `8840b2a`，不得把 PR OPEN 写成已合并，也不升级 PLC/CODESYS、HAL 或现场轴。
> **Stage 4 原子编辑命令与撤销基础 WP-147/148 正式关闭：2026-08-27**（`WP-20260827-148`，本段取代上条 WP-147 “待 fallback Reviewer 与后续 Claude 正式回审”的当前口径，历史叙述原样保留）：Claude 已对 `WP-20260827-147` 的 fallback 未审核候选（`src/editor/cfc_commands.py` 六个原子编辑命令 + `CFCEditResult` 单步 before/after 撤销基础 + `src/editor/__init__.py` 导出 + `tests/test_editor_cfc_commands.py`）完成正式回审：复算 7 scope 聚合 `852277…` 与 11 frozen 聚合 `6948…` 均与 WP-147 交付/审核终态逐字节一致、无漂移；产品复核确认单一 exact-shell 信任门禁在任何 `to_json` 前完整校验 document/model/node/pin/connection/layout 各层并拒绝直接构造的恶意壳体、实例 extra 字段与 BaseException 钩子（零观察），图/连接/carrier/read_mode/反馈/定序语义仍只由冻结 `load_cfc_document` 裁决，六命令成功返回全新不可变文档、失败原子不改输入/既有结果/冻结依赖，`CFCEditResult` 仅存 exact before/after。回审未发现须在 scope 内修改的缺陷，产品/测试三文件哈希保持 WP-147 终态。本轮亲自实跑 V0（compile 3/3、`src.editor` exact `__all__` 16/16）、V1 `tests.test_editor_cfc_commands` 67/67、V2（+6 CFC 邻接套件）210/210、`ParserTests` 27/27。WP-148 已经新的 Codex Round 2 独立审核判定 `APPROVED`，用户现确认将 WP-148 行政关闭，并同步收口其正式承接的 WP-147；两包当前均为 `CLOSED / user / user`。WP-147 的 Claude HTTP 429、`BLOCKED` 来源轴和 Fallback Lite 全部历史证据继续保留。当前产品边界仍仅是无界面六个原子编辑命令与单步 before/after 撤销基础；未提交/未合并、未完成 Stage 4，也不构成 UI、完整历史栈、持久化或 CODESYS/PLC、HAL、现场证明。
> **Stage 4 无界面 CFC 编辑文档模型 v1 正式关闭：2026-08-27**（`WP-20260827-146`，`CLOSED / user / user`）：新增 `src/editor` 包与 `cfc_document.py`，建立 Stage 4 首个无界面工程文档合同——exact-JSON 失败关闭边界保存平台**新建** CFC 图（只接受 `carrier=user_defined`/`order_source=user_defined`、`execution_order_mode` auto 或 explicit，拒绝 `plcopen_xml`/`export_native` 冒充编辑器新建图）、逐节点 layout（`node_id/x/y/comment`，x/y exact int 且 bool 不冒充 int、comment exact str，缺失/悬空/重复稳定诊断且顺序不随输入排列变化）、注释、确定性 JSON 往返、构造期容器别名封闭、双实例隔离，并只通过冻结 `load_cfc_model` 投影为不可变 `CFCModel`（再 `to_order_graph` 到 `CFCOrderGraph`），不复制节点/管脚/连接/carrier/反馈/定序规则、不猜 `read_mode`/`feedback_marker`。`src.editor.__init__` 只导出文档模型、诊断/错误、版本常量与 load/dump，不改 `src.runtime` 16 项 CFC 顶层 API、不做 `src` 根级导出。Claude Round 2 与 Codex Round 2 独立验证通过：V0 编译+`src.editor` 导入冒烟、V1 `tests.test_editor_cfc_document` **45/45**、V2（+6 冻结 CFC 套件）**161/161**、`ParserTests` **27/27**，冻结依赖聚合 `415b35ff…` 未变。这是 **Stage 4 约 1/5 的正式审核子范围**，不表示 UI、编辑增删改命令、undo/redo、文件持久化、ST 对照或 Stage 4 完成，也不构成 PLC/CODESYS、`.export` 自动重建、反馈真机语义、HAL 或现场证明；未提交未合并。
> **历史快照——Stage 4 原子编辑命令与撤销基础候选：2026-08-27**（`WP-20260827-147` 当时为 `BLOCKED / user / user`，`MANUAL_TRIAD_FALLBACK_LITE_V1`；当前状态已由上方 WP-148 正式审核与关闭记录取代）：当时的未审核候选新增 `cfc_commands.py` 的 six narrow commands（节点/连线添加与删除、节点移动、注释更新）和 `CFCEditResult` 单步 before/after undo/redo 基础；命令在调用 `CFCDocument.to_json()` 前以单一 exact-shell 门禁验证 `CFCDocument/CFCModel/CFCNode/CFCPin/CFCConnection/CFCLayoutEntry`，拒绝直接构造出的恶意 graph、layout 或 nested shell，避免观察 `graph.to_json` 或让 `BaseException` 逃逸。图/管脚/连接/carrier/read_mode/反馈/定序语义仍只由冻结 `load_cfc_document` 裁决；该历史候选不含历史栈、批量事务、持久化、UI 或 Stage 4 完成声明，后续已由 fallback Reviewer 和 WP-148 Claude/Codex 正式链完成审核。Python 证据不推导 PLC/CODESYS、HAL 或现场验证。
> **Stage 2 CFC 目录级正式收口：2026-08-27**（`WP-20260826-145`）：Claude 已把 WP-085～091 的 Stage 2 CFC fallback / BLOCKED 来源链形成的 15 文件作为未审核候选正式复核，Codex 独立审核与宿主补充反证随后通过，用户现已确认关闭。冻结边界为内部 `CFCModel v1`、按 carrier 分支定序、current→`LoadVar` / 显式 previous→`LoadPrev`、安全 `compile_cfc_task` 和 `src.runtime` 16 项 CFC 顶层合同；验证为 V0 4/4、V1 140/140、V2 476/476、V3 tests 2033/2033、`prototype_05` 68/68、根目录 2101/2101。该 Python 目录合同现可作为 Stage 4 依赖闸门；仍未提交未合并，也不构成 CODESYS/PLC、`.export` 自动重建、反馈真机语义、HAL 或现场验证。
> **Stage 3 ST 严格子集目录级正式关闭：2026-08-26**（`WP-20260826-144`，`CLOSED / user / user`）：Claude 已把 WP-135～143 的 fallback 候选作为未审核起点完成正式回审，Codex 独立审核及宿主补充反证最终 `APPROVED`，用户已确认关闭。当前冻结的 Python 合同包含 11 项 ST 顶层编译 API、4 个内部 ST 模块、8 个原语与 14/14 业务块显式 alias、通用库 `VAR_IN_OUT` 成功后原子写回、`required/use_default/keep_previous/none_means_no_write` 省略策略及任务级共享 `LicenseContext`；继续严格拒绝 IEC 隐式转换、动态 `FOR`、非有限源码字面量和默认 Registry 非有限 float。正式证据为 V0 10/10、V1 258/258、V2 802/802、V3 tests 2033/2033、`prototype_05` 68/68、根目录 2101/2101。该范围仍未提交未合并，也不构成 CODESYS/PLC、REAL binary32/整数回绕、动态现场 NaN/±Inf、HAL、真实 I/O、硬件 watchdog 或现场证明。
> **历史快照——Stage 3 WP-135～140 目录级备用候选：2026-08-25**：用户授权在 Claude 暂不可用期间按 `MANUAL_TRIAD_FALLBACK_LITE_V1` 顺序推进。当前候选已把 `APCRSFNAUTOPARA`、`APCMAUTOPARA`、`APCCD`、`APCM` 纳入显式 ST allow-list，使 8 个原语与 14/14 业务块均有 source alias；WP-137 复用既有 IR/Executor/Store 完成通用库 `VAR_IN_OUT` 读入/成功后写回，WP-138 复用既有 `none_means_no_write` / `keep_previous` 和共享 `LicenseContext` 接入 APCM。WP-136 纵向验收发现并窄修 `MAN_RESP_T_AUTO` 的 REAL 载体分支。WP-139 冻结当前严格子集：不猜 IEC 隐式转换，动态 `FOR` 稳定拒绝，静态 `FOR` 保持 100000 次预算，非有限源码字面量拒绝，默认 22-entry Registry 的浮点默认值必须有限；动态现场/HMI 非有限值的全局策略仍交数值/对拍阶段。WP-140 新增目录/API/依赖方向与代表性 TON 三拍验收；V1 **116/116**、V2 **1113/1113**、正式 tests **2016/2016**、`prototype_05` **68/68**、根目录 **2084/2084** 均通过。该段记录当时 `BLOCKED / user / user` 的候选状态，当前口径已由上方 WP-144 正式关闭记录取代。
> **Stage 3 第 8～10 个业务块 ST 接入正式回审关闭更新：2026-08-17**（`WP-20260817-130～134`）：Claude 已把备用机制形成的 `APCSPFINDER`、`APCPIDZZD`、`APCPID` 候选作为未审核内容完成累计正式复核，新的 Codex 独立审核 `APPROVED`，用户现已确认 `WP-130～134 = CLOSED / user / user`；WP-133 的无效 v2 交接历史原样保留，由 WP-134 的合法结构化自审与审核证据正式承接。WP-134 审核开始/结束时的五文件聚合均为 `fdb8e36e8d9f9a542bfdbacc0696613ba09b4f35dea7caf1a645e0148060d46c`，冻结 29 文件聚合均为 `cdfd97f5d491144c2bea2b0f112a671172c24f388cd3eeb0c1ae84e0310b41f2`；关闭后的风险文档仅作本段行政同步，四个产品/测试文件哈希未变。Claude/Codex 的正式证据为 V1 `242/242`、V2 `641/641`，另有 Codex 未预告 pin 失败矩阵 `15/15`、恶意 alias `1/1`、optional 省略 `7/7`、同/跨 Runtime 身份 `6/6`、授权失败恢复 `8/8`。`library_source_aliases()` 现以显式 allow-list 正式覆盖 **10/14** 个业务块；剩余 `APCRSFNAUTOPARA`、`APCMAUTOPARA`、`APCCD`、`APCM` 以及 `VAR_IN_OUT`、隐式转换、动态 `FOR`、工程级 import、ST/CFC 组合与 Stage 3 目录级总验收仍待后续工作包。Git/GitHub 尚未收尾，PLC/CODESYS、真实许可 Provider、HAL、真实 I/O 与现场语义仍未验证；本次关闭不表示 Stage 3 整体完成。
> **Stage 3 ST RETURN 与五个业务块正式回审关闭更新：2026-08-17**（`WP-20260814-122～125`）：Claude 已把备用机制形成的 `RETURN`、`APCSTATISTICS`、`APCHSFOP`、`APCHSRATELIM`、`APCHSACCUM`、`APCHXHCL` 候选作为未审核内容完成正式复核和返修，随后由新的 Codex 独立审核；WP-125 Round 3 最终为 `APPROVED`，用户现已确认 `WP-122～125 = CLOSED / user / user`。最终批准 scope 聚合保持 `1f2747a36d06bfcf16bb73e0d8ea358dbc4410ded47cb45e75ea48025d8e78a8`，冻结依赖聚合保持 `3a59b3dda296ce2248d385765f15891e2b1609796d0a6c6ceea9b78c49927138`；Codex 宿主复核为 V1 `116/116`、V2 `481/481`、未预告反证 `68/68`、ParserTests `27/27`，并修复 alias catalogue exact-dict/键类型门禁及未注册 alias 的稳定失败关闭。该更新只收口 `RETURN` 与上述五块子范围，不表示 Stage 3 整体完成：其余 9/14 业务块、`VAR_IN_OUT`、隐式转换、动态 `FOR`、工程级 import、ST/CFC 组合与目录级总验收仍待后续工作包；Schema `Pin.default` 的 NaN/±Inf 策略留待 Stage 3 目录级验收统一裁决。Git/GitHub 尚未收尾，PLC/CODESYS、HAL、真实 I/O 与现场语义仍未验证。
> **Stage 3 ST 前端目录级备用候选：2026-08-11**（`WP-20260810-092～112`）：内部 lexer/parser/typed-IR lowering 已覆盖声明、严格同型表达式/赋值、工程字面量、IF/CASE/FOR/WHILE/EXIT/CONTINUE、`ABS/MIN/MAX/LIMIT`、惰性 `SEL`、FUNCTION 与用户 FUNCTION_BLOCK 定义/调用；PROGRAM 可声明局部用户 FB，以及用逐项显式 alias 调用 8 个基础原语（例如 `PT→PT_ms`、`ET→ET_ms`）。WP-112 新增 provisional 顶层编译/错误/结果 11 项，并用同一代表性 PROGRAM 贯通 FUNCTION、用户 FB、TON、ABS、SEL、WHILE 的 compile→build_runtime→Executor 多拍路径；V1 **117/117**、V2 **490/490**、正式 tests **1939/1939**、`prototype_05` **68/68**、全仓 **2007/2007** 均通过。正式轴继续 `BLOCKED / user / user`，待 Claude 对 WP-092～112 正式回审、必要修改并交新的 Codex 独立审核；不得据此宣布 Stage 3 正式完成、IEC/CODESYS 一致、Git/GitHub、PLC/HAL 或现场证明。14 个工程业务块 source pin alias、可省略 formal、隐式转换、动态 FOR、RETURN、嵌套/数组实例、项目级 POU parser 与真实工程导入仍后置。
> **Stage 3 ST 前端早期历史快照：2026-08-10**（`WP-20260810-092～101`，已由上段当前摘要取代）：内部 lexer/parser/lowering 已支持六种 `VAR*`、当前 IR 全 17 种基础类型名、name/member/multi-index/call 源 AST、严格同类型表达式与简单赋值；已可执行 BOOL/十进制 INTEGER/REAL/TIME/STRING 工程字面量，以及 `IF`、`CASE`、有界常量 `FOR`（含嵌套、范围/重叠/溢出/迭代上限失败关闭）。这些子集均 lower 为经 `validate_task` 的 typed IR，并已有 `build_runtime→Executor` 多拍与独立手写 IR 逐值对照；ST 定向 **75/75**、ST+IR/Store/Executor/parameters 邻接 **373/373**。`WP-101` 另在 `execute_programs` 公开入口建立跨 PROGRAM、FUNCTION 和用户 FB 共享的 1,000,000 条确定性 IR 指令预算，Executor **145/145**、跨运行时邻接 **267/267**、装配邻接 **105/105**；该预算不等于墙钟 deadline 或单个不可信调用的抢占。动态 FOR 边界、`WHILE/EXIT/CONTINUE`、member/index/call 执行、标准函数、IEC 隐式转换和完整 POU/实例绑定仍后置。正式轴保持 `BLOCKED / user / user`，待 Claude 对 WP-092～101 正式回审、必要修改并交新的 Codex 独立审核；不构成 Stage 3、IEC/CODESYS、PLC/HAL 或现场一致性证明。
> **Stage 3 ST 词法基线候选：2026-08-10**（`WP-20260810-092`）：新增内部 `src/runtime/st_lexer.py`，建立 exact-string 失败关闭边界、不可变 token/诊断、确定源位置、ASCII IEC 标识符/关键字（含 CODESYS `EXPT`）、最长匹配运算符、十进制整数/REAL、`T#`/`TIME#`、STRING 及 line/nested-block comment 的 Stage 3 最小词法子集。字面量仅保留原文与规范形，尚未解析数值；typed/base literal、numeric separator、Unicode identifier、AST、类型推导、声明/实例绑定与 IR lowering 均未实现。未支持的 `1_000`/`123abc` 不会被拆成多个伪合法 token，而是稳定失败关闭。当前为 Codex 备用实施候选，正式轴仍 `BLOCKED / user / user`，待 Claude 回审和新的 Codex 独立审核；不构成 Stage 3、IEC/CODESYS、PLC/HAL 或现场一致性证明。
> **Stage 2 CFC 目录验收与 provisional 顶层 API 用户裁决后窄修候选：2026-08-10**（`WP-20260810-091`）：`src.runtime` 仅新增**16 个** CFC 模型、load/dump、编译入口/结果及模型、lowering、定序诊断/错误的精确顶层重导出；低层 order graph、direct-lowering 载体与入口继续只允许从子模块使用。Round 1/2 为 `REVIEW_CHANGES_REQUESTED`，Round 3 Reviewer 随后发现三项仅位于契约测试的证明缺口；用户指出“按轮次数机械永久停工”不合理，主控复核正式 Fallback Lite 规范后确认阈值应触发强制复盘/重新规划，而非忽略问题性质。经用户授权的极窄修复现已补齐 absolute `ImportFrom` parent-package alias、lambda definition-time defaults 和九项 internal 定义来源/名称/类型证明，三项控制变异均由漏过转为拒绝；未修改 `src.runtime` 或冻结 CFC 实现。V0 **2/2**、完整 V1 **83/83**、V2 **555/555**，V3 正式 tests **1818/1818**、`prototype_05` **68/68**、根目录 **1886/1886**，`git diff --check` 通过。该结果是 Codex 用户授权修复候选，不是独立批准；正式轴仍 `BLOCKED / user / user`，必须 Claude 正式回审、必要修改并交新的 Codex 独立审核；不构成 Stage 2 完成、正式 API 冻结、Git/GitHub、PLC/CODESYS、HAL 或现场证明。
> **阶段 2 CFC→Runtime 纵向验收 Fresh Reviewer Round 3 口径：2026-08-10**（`WP-20260810-090`）：在冻结的 `compile_cfc_task(...).task → build_runtime(...) → Executor.execute_programs(prev_snapshot)` 公开路径新增 9 项 Python 纵向反证。Round 3 窄返修已使 72 个排列逐个以 fresh default Registry/Runtime 和 fresh writes 跑四拍、按键排序写入；持久锁定失败后 caller-held `prev` 不前移且修正后 `LoadPrev` 仍读原快照，并以三套 TON Runtime 的 Timer 实例身份及不同节奏 ET 证明隔离。fresh Reviewer Round 3 已 `FALLBACK_APPROVED_PENDING_CLAUDE`；正式轴仍为 `BLOCKED / user / user`，仅待 Claude 正式回审；不构成正式批准、关闭、Git/GitHub、阶段 2、PLC/CODESYS、HAL 或现场证明。
> **阶段 2 CFC 信任边界最终恢复候选：2026-08-09**（`WP-20260809-088`）：基于 WP-087 的私有 `_OutputAccessMap`，当前五文件 Fallback Delivery 候选以统一 `dataclasses.fields()` instance-shell 证明覆盖实际读取的 Task/声明/POU、CFC direct-lowering 和正式 IR dataclass；Registry 作为唯一非 dataclass 例外仅接受 exact `{'_entries'}`，并从经证明的 Pin/Schema/Adapter inert 字段及 carrier `_pairs` 重建新 Registry。`initial`、whole `ctor_args`、whole `init_overrides` 分根 bounded clone（32 层、4096 occurrence slots，cycle/shape/unsupported leaf 首错停访，合法 DAG 只在根内保留共享）；opaque source/policy/dependency/callable 保持身份。当前 CFC compile 定向为 **18/18**，含 default Registry 22-entry hook-free copy 成功与 carrier/字段/预算反证。过程证据限制：新加反证未在未修改的 `21ea5c4e…` 候选逐项实跑；唯一已实跑旧套件红证为 **13/14**、其一为预期 `MappingProxyType` compatibility red，独立 Reviewer 须以盲测补偿。**WP-086 保持两轮 `CHANGES_REQUESTED` 后 `REPLAN_STOP`；WP-087 仅为 provisional fallback approval；WP-088 已被 Reviewer 判 `CHANGES_REQUESTED` 并触发 hard stop，由 WP-089 承接；正式轴仍 `BLOCKED / user / user`，待 Claude 正式回审。**不构成正式批准、关闭、Git/GitHub 或阶段 2 完成；Python 证据不构成 PLC/CODESYS、HAL/现场证明。
> **CFC direct-feedback 可信 Registry 恢复候选：2026-08-10**（`WP-20260810-089`）：在 WP-088 Reviewer `CHANGES_REQUESTED` + hard stop 后、经用户明确授权建立的极窄恢复候选，使 feedback 入口保存 `_validate_registry` 的可信副本，避免末次校验回触 source Registry；正式轴仍 `BLOCKED / user / user`，待 Claude 正式回审和新的 Codex 独立审核，不构成批准、关闭、Git/GitHub 或阶段 2/PLC/CODESYS/HAL/现场证明。
> **阶段 2 CFC 安全编译入口 Fallback Iteration 2 历史候选：2026-08-09**（`WP-20260809-086`）：在既有 `compile_cfc_task` 内补齐“exact 外壳不等于可信对象图”的同源边界：声明配置仅接受有界、cycle-aware 的 exact 内建叶/容器图（最大 32 层、每根 4096 节点，DAG 共享保留；环、超限、opaque leaf 失败关闭）；`VarDecl.retain/persistent` 与 `IOMap.channel` 纳入 exact 门禁/重建；Registry 在零观察证明 exact entry、Pin/BlockSchema 纯数据与 RuntimeAdapter 结构后重建为新 exact Registry，再交冻结 lowering/Loader。adapter/schema hook、source/policy/dependency/callable 皆不执行或复制，保持 opaque 身份边界。`tests/test_runtime_cfc_compile.py` 曾含 14 项。独立 Reviewer 结论为两轮 **`CHANGES_REQUESTED`**，随后因 `MappingProxyType` 无可移植零观察 backing 作 **`REPLAN_STOP`**；该历史候选保持冻结、正式轴仍为 `BLOCKED / user / user`。
> **阶段 2 内部 CFC 模型 Loader 审核关闭更新：2026-08-08**（本段取代紧随其后的“候选更新”当前口径；其逐轮过程与历史测试数字原样保留。`WP-20260808-082` 已完成 Claude Round 4 v2 自审与原子交接、Codex Round 4 独立审核 `APPROVED`，并经用户确认 `CLOSED`。最终五文件审核聚合为 `61b80700eb571389fab49873ac7f52576f91b0f71a1a6a28521314e68f9f2922`，审核前后无漂移；模型 50/50、CFC 四组 106/106、IR+Executor 204/204、导入试验 13/13、ParserTests 27/27、两文件内存编译与 `git diff --check` 均通过。阶段 2 CFC 定序/lowering 内核与内部模型 Loader 现均已审核关闭，但仍是本地未提交、未合并候选；下一步为 WP-083/084 的真实入口接线与阶段 2 纵向验收，不得据此宣称阶段 2、PLC/CODESYS、HAL 或现场验证完成。）
> **阶段 2 CFC 定序内核关闭与内部模型 Loader 候选更新：2026-08-08**（本段取代下方 §2「下一步（边界分离）」中“Fallback Lite / 阶段 2 CFC 编译器内核正式回审待办”的当前口径；历史段落与测试数字原样保留。阶段 2 CFC 定序 / typed IR lowering 内核候选已由 `WP-20260806-079 CLOSED`（Round 3 正式 Claude 回审 + Codex 独立审核 + 用户关闭）与 `WP-20260808-080 CLOSED`（WP-079 Round 4 中断检查点恢复、完整 CFC 输入图零观察收口）收口；`src/runtime/cfc_order.py`/`src/runtime/cfc_lowering.py` 及三份对应测试为**已审核关闭但本地未跟踪、未提交、未合并**的 Python 部分实现，Git/GitHub 收尾另行执行、当前主线仍为 `8840b2a…`。`WP-20260808-081` 曾在单一新模块 `src/runtime/cfc_model.py` 新增阶段 2 内部 CFC 模型 v1 与安全 `load_cfc_model` Loader，但在合法 v2 自审与原子交接前触发 HTTP 429 配额中断、未交接，已如实封存为 `BLOCKED / user / user`；承接包 `WP-20260808-082`（本包）以该精确中间检查点（scope 聚合 `deda6e80…`、逐文件 manifest 与冻结依赖复算一致无漂移）为首轮 baseline 复核该 Loader：Round 1 复核后合法交接，Codex Round 1 独立审核判 `CHANGES_REQUESTED`（四项必须返修：非法图诊断随输入排列漂移、测试未锁完整诊断排列无关、真实 `.export` 样本事实与合成能力反证混写、状态文档口径与后续包号）；**Round 2 已按同源根因返修**——`load_cfc_model` 改为两阶段"先物化再分组唯一键"，使重复 node_id / 重复显式序号诊断与连接语义排列无关，新增完整诊断（含 node_id 归因）排列反证，拆分真实样本 fixture（保留观测到的 `IsFeedbackStart=False`）与明确标注的合成能力 fixture，并同步本三份状态文档，亲自复跑全部指定测试通过后再交 Codex Round 2 独立审核；**Codex Round 2 判 `CHANGES_REQUESTED`（两项：公开数据对象容器别名未封闭、页首功能矩阵 CFC 口径与 USR-01 冲突），Round 3 已按两项意见返修**——为 `CFCNode.pins` / `CFCModel.nodes` / `CFCModel.connections` 增构造期零观察容器规范化（exact `tuple` 原样保留、exact `list` 复制成新 `tuple`、其它类型失败关闭），封闭 `frozen=True` 挡不住的调用方可变容器别名并新增直接构造别名反证，同时把 `docs/SOFT_PLC_FUNCTION_MATRIX.md` 页首 CFC 口径与 `USR-01` 行对齐，再同步本三份状态文档、亲自复跑全部指定测试通过后交 Codex Round 3 独立审核；**Codex Round 3 判 `CHANGES_REQUESTED`（唯一必须返修：`docs/RISKS.md` 页首当前摘要滞后未同步 Round 3），Round 4 为纯文档收口**——把 `docs/RISKS.md` 页首当前摘要连同本三份状态文档的当前候选口径统一同步为 Round 3 `CHANGES_REQUESTED` 后 Round 4 收口，零代码/测试改动、反证仍 50。该模型 Loader（不可变 `CFCModel/CFCNode/CFCPin/CFCConnection`、exact-JSON 失败关闭边界、四类可执行/保存载体组合、顺序/连接规范化、JSON 往返、原 payload 不变、双实例隔离、到 `CFCOrderGraph` 的只读投影），配 `tests/test_runtime_cfc_model.py` 50 项正向 / 边界 / 恶意对象 / 完整诊断排列无关 / 直接构造别名反证（Round 2 由 45 增至 48、Round 3 增至 50）。它是**模型 Loader 实施候选、WP-082 Round 4 待 Codex 独立审核**，**不得**写成 WP-081/082 APPROVED/CLOSED、阶段 2 完成或已提交 / 已合并。Loader **不**据序号 / 拓扑 / 名称 / carrier 猜测反馈边，`feedback_marker` 与逐连线 `read_mode` 分层且两者映射本包不冻结，PLCopen 无 marker 归一内核默认 False，`export_native` 组合仍由现有内核门禁拒绝。下一步：WP-082 Codex 独立审核收口后，由后续 WP-083/084 把 PLCopen / `.export` 导入器与平台新建图接线到本模型，再进入阶段 3+。Python 证据不构成 PLC/CODESYS、真实调度、HAL/物理 I/O、硬件 watchdog、黄金轨迹或现场安全证明。）
> **阶段 1 / M1 Git/GitHub 收口更新：2026-08-06**（本段取代下方 2026-08-05「阶段 1 / M1 GitHub 发布候选」段的当前口径；历史段落与测试数字原样保留。阶段 1 / M1 与 `WP-20260805-075` 已批准工程支持的发布候选已通过 [PR #34](https://github.com/yao501/PLC_to_Python/pull/34) 合并，merge commit=`8840b2a443e466ed8d0192defa1a4545907b3039`（GitHub `mergedAt=2026-08-05T16:01:44Z`，Asia/Shanghai 记为 2026-08-06）；候选 head=`3598c051fd6fa531ca08ea9e6e8fa5d938b897cb` 与 merge commit 的 Git tree 均为 `fd0e281788bef85dcd768ea03969013426e843e7`。收口后 `main == origin/main == HEAD == 8840b2a443e466ed8d0192defa1a4545907b3039`。已审核候选的精确测试事实原样保留、非合并后重跑：阶段 1 定向 **71/71**、系统 Python 3.9 定向 **60/60**、AI Handoff **202/202**、正式 tests **1663/1663**、`prototype_05` **68/68**、全仓 **1731/1731**、`py_compile` 6/6、公开导出 6/6。六个 Fallback Lite / 阶段 2 CFC 候选（`docs/MANUAL_TRIAD_FALLBACK_LITE.md`、`src/runtime/cfc_order.py`、`src/runtime/cfc_lowering.py` 及三份对应测试）继续保持本地未跟踪、哈希冻结、待正式 Claude 回审，未进入本次发布，不得写成已合并或阶段 2 已完成。下一步先完成 Fallback Lite 正式回审，再完成阶段 2 CFC 编译器内核正式回审。Python 证据不构成 PLC/CODESYS、真实调度、HAL/物理 I/O、硬件 watchdog、黄金轨迹或现场安全证明。）
> **阶段 1 / M1 GitHub 发布候选更新：2026-08-05**（功能边界已达成《PLATFORM_ROADMAP》的阶段 1 headless MVP 验收：22/22 adapter、正式 IR/Store/Executor/五步扫描、OutputPolicy/故障安全提交/默认 shadow、参数与启动校验、软件 monitor、确定性 startup inhibit/readiness 及单任务跨组件验收均有 Python 实现和审核证据。`WP-20260804-072 CLOSED` 已收口阶段 1 最后的 readiness 故障锁存优先级缺口；`WP-20260805-075 CLOSED` 已收口 60 分钟执行墙钟、轮次准入与 JSONL 失败关闭工程支持。当前发布基线仍为 `main == origin/main == HEAD == aa73c56b5f07ed5bd87a6b0c4447d137f0474b04`；精确 Git 暂存候选已导出到不包含本地 CFC/Fallback 文件的独立目录验证：阶段 1 定向 **71/71**、系统 Python 3.9 定向 **60/60**、AI Handoff **202/202**、正式 tests **1663/1663**、`prototype_05` **68/68**、全仓 **1731/1731**、`py_compile` 6/6 与公开导出 6/6 均通过。本段记录的累积修改是**已批准、待 Git/GitHub 合并的发布候选**。本次明确排除仍待 Claude 正式回审的 Fallback Lite 与阶段 2 CFC 六文件候选；它们保留本地、不进入本次发布。Python 证据不构成 PLC/CODESYS、真实调度、HAL/物理 I/O、硬件 watchdog 或现场安全证明。）
> **Claude Runbook Git/GitHub 收口更新：2026-07-30**（`WP-20260730-052` 已从 WP-051 Round 3 中断检查点完成剩余返修，经 Claude 结构化 v2 自审、Codex 独立审核 `APPROVED` 和用户确认 `CLOSED`；Runbook、首轮/返修统一 prompt、`--max-turns 80` 与契约测试已通过 [PR #32](https://github.com/yao501/PLC_to_Python/pull/32) 合并，merge commit=`252842f4cffd9056085524a2dcc341f5908016c9`。功能合并后 `main == origin/main == HEAD == 252842f…`；最新完整宿主快照为正式 tests **1568/1568**、`prototype_05` **68/68**、全仓 **1636/1636**，Runbook 冒烟与 `git diff --check` 通过。WP-041/043/044/046/047/049/051 的原始阻塞或返修记录均保留，但其未完目标已有后继闭合，顶层行政状态已统一为 `CLOSED`，解析器当前指向 WP-052。该能力是工程协作基建，不是软 PLC 产品功能，也不构成 PLC/CODESYS、HAL 或现场证明。下一产品工程主线为阶段 2 CFC 数据流定序编译器。）
> **阶段 1 单任务装配 Git/GitHub 收口更新：2026-07-30**（本段取代下方 WP-049/050 候选、待审核、未提交/未合并的当前行政口径；历史过程和测试数字原样保留。`WP-20260730-050` 承接 WP-049 配额中断检查点，经 Claude 结构化 v2 自审、Codex Round 3 独立审核 `APPROVED` 和用户连续授权确认 `CLOSED`；其八文件产物已通过 [PR #30](https://github.com/yao501/PLC_to_Python/pull/30) 合并，merge commit=`73b462b5b0838e5826cbcd59db0b37a1893e44ad`。收口后 `main == origin/main == HEAD == 73b462b…`；最新完整主机快照为正式 tests **1560/1560**、`prototype_05` **68/68**、全仓 **1628/1628**，导入冒烟与 `git diff --check` 通过。`build_task_runtime` 仍是 Python 内部/测试装配入口，不是最终 ST/CFC 用户入口；真实调度、多任务、startup inhibit 计时/释放、外部信号源、CFC 定序、HAL/I/O、硬件 watchdog、CODESYS 对拍与现场安全仍未实现或未验证。下一步先执行独立协作基建包 WP-051（Claude 实施 Runbook 与启动器强制阅读），之后产品工程主线进入阶段 2 CFC 数据流定序编译器。）
> **软件 monitor Git/GitHub 收口更新：2026-07-29**（本段取代下方 WP-048 关闭时点的“未提交、未合并”当前口径；历史段落和测试数字原样保留。WP-043～048 累积 monitor、WP-045 功能矩阵及 `max_rounds: 5` 协议说明已通过 [PR #28](https://github.com/yao501/PLC_to_Python/pull/28) 合并，merge commit=`c5031fff9a35fbf724ea74f680bc3a1276af2555`；收口后复核 `main == origin/main == HEAD == c5031ff…` 且工作区干净。最新已合并且已关闭的完整主机基线更新为正式 tests **1537/1537**、`prototype_05` **68/68**、全仓 **1605/1605**；下一工程目标为阶段 1 端到端验收与真实任务装配。上述仍只是 Python 主机证据，不构成真实调度、连续 deadline miss、硬件 watchdog、HAL/I/O、CODESYS SP16.1 或现场安全证明。）
> **软件 monitor 审核关闭更新：2026-07-29**（本段取代下方 WP-043～048 各实施/返修时点的“候选、待审核”当前口径；历史段落与测试数字原样保留。`WP-20260729-048` 已由 Claude 完成结构化 v2 自审与原子交接、由 Codex Round 2 独立审核为 `APPROVED`，并经用户确认 `CLOSED`；`WP-20260729-043/044/046/047` 的 `BLOCKED` / `CHANGES_REQUESTED` 继续作为被后续承接包收口前的历史检查点，不回写。当前 Python 确定性软件事件源具备注入式整数纳秒时钟、周期观测、阈值锁存、一次性派发、exact-int 与不可信公开 dataclass 零观察边界；最终宿主快照为 monitor **96/96**、正式 tests **1537/1537**、`prototype_05` **68/68**、全仓 **1605/1605**。当前累积改动仍在工作区，**尚未提交、尚未合并**，因此 1605 尚不是主线基线；Git/GitHub 收尾另行执行。真实实时循环/调度、连续 deadline miss 升级、在途扫描异步抢占、进程/OS 崩溃恢复、硬件 watchdog、HAL/真实 I/O、CODESYS SP16.1 与现场安全仍未验证，相关风险不转 resolved。）
> **AI 协作轮次政策更新：2026-07-29**（用户裁决：自 `WP-20260729-048` 起，新工作包默认显式使用 `max_rounds: 5`；历史工作包已记录的 `max_rounds: 3`、历史轮次和 `BLOCKED` 结论原样保留。调度器本来就读取每包显式字段通用比较，无需修改状态机代码；增加轮次不扩大 scope、Git/删除、规格裁决、外部系统权限或 Python→PLC/HAL/现场证据边界。）
> **monitor pending 别名零观察收口候选：2026-07-29**（承接包 `WP-20260729-048`，`max_rounds: 5`，Round 1，收口 `WP-20260729-047` Round 3 Codex 独立审核确认的第八项缺陷：内部保留的 pending `WatchdogTimeoutEvent` 与交给调用方的是**同一实例**、`frozen` 挡不住 `object.__setattr__`，`begin_cycle` 旧实现以 `%d` 读取 `self._pending.sequence`，字段被换成抛 `BaseException` / 带副作用对象时诊断逃逸或被污染。已改用不暴露给调用方、由 exact-int 内部路径维护的可信 `_latched_seq`，**绝不读取 pending 任何公开字段**；`WatchdogTimeoutEvent` 与模块 docstring 明确公开事件字段对内部不可信、一次性语义由内部 `_pending`/`_latched_seq` 保证。不新增功能、不改公开导出（`src/runtime/__init__.py` 冻结哈希 `6dc0b881…` 不变、不在 scope）。`tests/test_runtime_monitor.py` 由 86 增至 **96**（Round 1 +10 pending 别名零观察反证）。Claude 亲自宿主复跑（本机允许绑定本地端口，`test_ai_handoff` 全绿）：monitor **96**、+scan_runner+output_policy **214**、安全运行时组 **240**、parameters+executor **192**、`test_ai_handoff` **147**、`discover tests` **1537**、`prototype_05` **68**、全仓 **1605**，导入冒烟 OK。此为**工作区候选，未提交、未合并、未经 Codex `APPROVED`、未经用户 `CLOSED`**；`1605` 不是已批准主线基线，最新已合并已关闭完整主机基线仍为 WP-042 的 1441/1441、`prototype_05` 68/68、全仓 1509/1509。）
> **monitor 四项返修候选：2026-07-29**（`WP-20260729-045` 功能矩阵已由用户确认 **`CLOSED`**；承接包 `WP-20260729-046` Round 1 返修 `WP-20260729-044` Codex 两项阻塞项——① 同一 active sequence 事件二次生成/callback 二次调用，已加**独立于 pending 槽**的 sequence 终态 `_latched_seq`；② 配置值/时钟接受可重载 `int` 子类，已改 **exact-int** 失败关闭——Round 2 返修 Codex 本包 Round 1 阻塞项③：exact-int 拒绝路径原以 `%r` 格式化被拒 `int` 子类，可重载 `__repr__` 在信任边界诊断阶段抛异常逃逸，已改经 `_safe_type_name()` 只报告可信类型名——**Round 3（用户授权恢复最后一轮，round=3=max_rounds）**返修 Codex 本包 Round 2 阻塞项④：Round 2 的 `_safe_type_name()` 直接返回 `type(value).__name__`，自定义 metaclass 可重载类型对象 `__getattribute__`（或把 `__name__` 定义成数据描述符）令其返回 `__str__` 抛异常的恶意对象、诊断 `%s` 再次执行攻击者代码逃逸；已改经 `type.__getattribute__` **绕过自定义 metaclass** 取候选类名、仅当 `type(name) is str` 才用、否则回退固定占位符。均不新增功能、不改公开导出（`src/runtime/__init__.py` 冻结哈希不变、不在 scope）。`tests/test_runtime_monitor.py` 由 39 增至 **63**（Round 1 +11、Round 2 +6、Round 3 +7 反证）。Round 3 Claude 亲自宿主复跑（本机允许绑定本地端口，`test_ai_handoff` 全绿）：monitor **63**、+scan_runner+output_policy **181**、安全运行时组 **240**、parameters+executor **192**、`test_ai_handoff` **147**、`discover tests` **1504**、`prototype_05` **68**、全仓 **1572**，导入冒烟 OK。此为**工作区候选，未提交、未合并、未经 Codex `APPROVED`、未经用户 `CLOSED`**；`1572` 不是已批准主线基线，最新已合并已关闭完整主机基线仍为 WP-042 的 1441/1441、`prototype_05` 68/68、全仓 1509/1509。）
> **功能矩阵与 monitor 候选登记：2026-07-29**（`WP-20260729-045` 已落库 `docs/SOFT_PLC_FUNCTION_MATRIX.md`——功能点级状态索引，六轴分离。**monitor 真实状态**：`WP-20260729-043` 产生的 `src/runtime/monitor.py` 及导出/39 项定向测试/RISKS 叠加是**工作区候选，未提交、未合并、未审核通过**（该轮实施交接被解析器判 `v2-invalid`）；`WP-20260729-044` 已恢复合法 v2 证据但 **Codex Round 1 `CHANGES_REQUESTED`**（两项必须返修：同一 active sequence 二次 poll/dispatch 致二次安全提交；配置/时钟接受可重载 `int` 子类绕过闸门伪造事件）。候选环境 `1548` 计数**不是**已批准主线基线；最新已合并已关闭完整主机基线仍为 WP-042 的 1441/1441、`prototype_05` 68/68、全仓 1509/1509。）
> **Git/GitHub 收口更新：2026-07-29**（本段取代下方 WP-042 关闭时点及更早工作包的行政快照；历史段落与测试数字原样保留。`WP-20260729-042` 已由 Codex Round 2 独立审核为 `APPROVED`、经用户确认 `CLOSED`，其承接的 WP-041/042 参数装载与启动失败关闭改动已通过 [PR #26](https://github.com/yao501/PLC_to_Python/pull/26) 合并，merge commit=`495ebb1e3dc7ae457e4986f3024d0bf266d0278a`；行政同步开工复核 `main == origin/main == HEAD == 495ebb1…` 且工作区干净。最新完整主机快照仍为正式 tests 1441/1441、`prototype_05` 68/68、全仓 1509/1509；下一工程目标为软件 monitor / 周期超时 / watchdog 事件源。`RUNTIME-PARAM-VALIDATION` 总项仍为 `in-progress`，Python 证据不构成外部配置、动态值、持久化、F2、CODESYS、HAL/I/O、硬件 watchdog 或现场安全证明。）
> **参数装载与启动校验关闭更新：2026-07-29**（`WP-20260728-041` 的 `BLOCKED / round=3=max_rounds` 作为历史原样保留；其三个已确认启动失败关闭缺口由窄范围承接包 `WP-20260729-042` 修复，Claude 完成 v2 自审/交接，Codex Round 2 独立审核 `APPROVED`，用户已确认 `CLOSED`。当前已建立公开 `build_runtime` 启动装配入口、IR/L2 静态错误聚合、显式实例构造覆盖/Store 初值/时间目录/startup inhibit 配置校验，以及递归 user-FB、非法 `numeric_mode`、APCHSACCUM 直连非法构造值的稳定失败关闭；最后完整主机快照为正式 tests 1441/1441、`prototype_05` 68/68、全仓 1509/1509。`RUNTIME-PARAM-VALIDATION` 整体仍为 `in-progress`：非 APCHSACCUM 构造目录、运行期动态值、外部配置解析/优先级、HMI、RETAIN/PERSISTENT、monitor/watchdog、HAL/I/O、CODESYS 对拍和现场安全均未完成。下一工程目标转为软件 monitor / 周期超时 / watchdog 事件源；Python 主机证据不构成 PLC/CODESYS 或现场发布证明。）
> **Git/GitHub 收口更新：2026-07-28**（本段取代下方 WP-040 实施交接时点的“待审核 / 未合并”行政快照；该旧段作为历史原样保留。`WP-20260728-040` 已由 Codex 独立审核为 `APPROVED`、经用户确认 `CLOSED`，并通过 [PR #24](https://github.com/yao501/PLC_to_Python/pull/24) 合并；merge commit=`8351fdf475efdd933c8bec22c4617056b5a4d1c2`。行政同步开工复核 `main == origin/main == HEAD == 8351fdf…` 且工作区干净。L2 **22/22 engineering adapter 目录**已审核关闭；下一工程工作包为**参数装载与启动校验**。最终主机快照仍为正式 tests 1383/1383、`prototype_05` 68/68、全仓 1451/1451；Python 证据不构成 F2、CODESYS、HAL/I/O、watchdog 或现场安全证明。）
> **最后更新：2026-07-28**（`WP-20260728-040` 对当前 22/22 engineering adapter 实现完成**独立目录级验收**：在 `tests/test_runtime_descriptors.py` 新增集中式 22 键目录反证——独立复算精确注册键集合、逐项 Schema 可 JSON 序列化 / `block_type`/`variant`/`descriptor_version` 完整 / Schema-Adapter 绑定一致 / 每输入合法 OmitPolicy / 每 `VAR_OUTPUT` 有且仅一条可解析 `output_access` 且非输出不被冒充 / `engineering ≡ fidelity_f1` / `fidelity_f2` 全部 `MissingVariantError` / 构造依赖图精确锁定（仅 `APCM`/`APCPID`/`APCPIDZZD` 需 `license_context`，其余 19 项不暗含依赖，`APCPID` 内嵌 `PIDZZD1` 与顶层共享同一注入 ctx、异图不串扰）；在 `tests/test_runtime_executor.py` 新增**可机器解析的 22 项行为覆盖矩阵** + 要求 6 全部横切语义索引 + 逐项 `Registry→Loader` 布局反证，把既有代表性/七原语/五基础/七复杂「直接调用 vs Registry→Loader→Store→Executor 逐拍对照」连成完整目录证据，不重复大规模逐拍测试。本包实跑九组命令全绿：正式 `tests` **1383/1383**、`prototype_05` **68/68**、全仓 **1451/1451**（另 descriptors 55、executor 135、runtime 四件 296、块+原语 605、运行时引擎组 240、`test_ai_handoff` 147 均 `OK`）。**Git 事实（如实）**：当前 `main == origin/main == HEAD == 72d32ea45179eb3af9bc5e0c5ceb0b99f1851108`；[PR #22](https://github.com/yao501/PLC_to_Python/pull/22)/`da6ff139c32baead628ce5050db79c9752af52a9` 只是 10/22 时期的历史已合并节点；当前工作区包含 `WP-026`～`039` 已审核但**尚未做 Git/GitHub 收尾**的累积改动加本包新增测试，工作区**并不干净**、这些改动**尚未合并**，须经用户授权由 Codex 审核暂存与提交。仅本包目录验收全绿即把「L2 22/22 engineering adapter 目录」记为**已验收**（待 Codex 独立审核收口）；F2、参数装载、`monitor/watchdog`、HAL、真实 I/O、可信反馈、RETAIN/PERSISTENT、CODESYS SP16.1 对拍与现场安全仍为独立未验证边界，`PLATFORM-EXEC-STORE-ATOMICITY-1` 局部 in-progress 边界不因目录验收转 resolved。以上均为 Python 主机证据，不构成 PLC/CODESYS/HAL/I/O/现场安全一致性证明。）
> **历史更新（2026-07-27）**：（`WP-20260724-025` 已按 v2 三阶段机制由 Claude 完成合法自审与原子交接、由 Codex Round 1 独立审核为 `APPROVED`，并由用户确认 `CLOSED`。七原语 engineering adapter、十键默认 Registry 及对应测试/风险/路线状态已通过 [PR #22](https://github.com/yao501/PLC_to_Python/pull/22) 合并，合并提交为 `da6ff139c32baead628ce5050db79c9752af52a9`；本次行政同步开工复核 `main == origin/main == da6ff139` 且工作区干净。L2 当前为 10/22，剩余 12 个业务块 adapter；最新完整主机快照为正式 tests 1299/1299、`prototype_05` 68/68、全仓 1367/1367。上述均为 Python 主机证据，不构成 PLC/CODESYS、真实 HAL/I/O 或现场安全一致性证明。）
> **临时高优先级修复更新（2026-07-22）**：现场趋势反证发现 APCM 原 ST 与 Python 转写的 `ZLEN/R_TRIG02` 整理不是原子动作，可能在清零 RSF 后同拍命中 AO4，并让 PID 从独立旧 `TP` 重建。用户授权 Codex 直接维护：桌面 `APCM20260722.txt` 与 `src/blocks/apcm.py` 已同步改为冻结 `ZL_PID_BASE`、完整复位/重新武装 RSF、事件拍冻结预限幅/CD 支路、PID 只从冻结值重建，`R_TRIG02` 每拍推进；整理专用位置式基准不单独限幅，避免 PID+RSF 与反向 CD 抵消时破坏组合总量。Claude 独立对抗审核结论为 `APPROVED_WITH_CONDITIONS`、P0/P1 均无；其 P2 测试保护与行为落档建议经 Codex 复核成立，已新增 5 个测试并扩展 ZLEN 重开反证，未再改变 ST/Python 控制语义。原 ST 备份 SHA-256=`80933acb254add72a9702e27b8d3f6af610f0436c7a75e61f143551d40c59e46`，修复版 SHA-256=`b29dfcf9eb5de3c79ac5c7f4ef2a92d7f7ec9f7511140d8490f68f41c99bd128`，CRLF 保持。APCM 63/63、相关链 206/206、正式 tests 1182/1182、全仓 1250/1250 通过；开工基线 `main=origin/main=2bab893`（PR #18 merge）。仓库变更的独立 Git/GitHub 收尾载体为分支 `codex/apcm-atomic-cleanup`、功能提交 `42c7a17` 与 [PR #19](https://github.com/yao501/PLC_to_Python/pull/19)。**尚未在 CODESYS SP16.1 中导入编译、仿真或真机验证；外部 ZLOUT 消费回路、增量输出解释与 ZLEN 现场启用条件仍须对拍，不得仅凭 Python 测试直接部署现场。**协调器与旧轮询保持停止/暂停。
> **主线更新（2026-07-23）**：`WP-20260723-015` Round 3 已获 Codex `APPROVED` 并由用户确认 `CLOSED`，收口了零配置默认 write-disable shadow 栈、普通对象图写门/底层提交能力旁路、shadow 故障/watchdog 逻辑采用、shadow→实写全通道 `safe_value` 边界重建及对应反证。最终独立验证为定向 240/240、既有运行时 166/166、正式 tests 1222/1222、`prototype_05` 68/68、全仓 1290/1290，`git diff --check` 通过；八文件审核哈希始终为 `f96d2a053bb4c7596ec33dd5c53368e14c962e27a25fbb3207a5a42caea991bf`。**Shadow 的 Python 核心契约已审核关闭，但 `RUNTIME-SHADOW-MODE` 风险仍为 in-progress：真实 HAL/可信反馈、实时 monitor、硬件 watchdog、真实驱动、PLC/CODESYS 对拍和现场安全证明均未完成，不构成现场发布授权。**协调器与旧轮询保持停止/暂停。
> **最近关闭工作包（2026-07-25）**：`WP-20260724-025` 已恢复合法 v2 自审并由 Codex Round 1 独立审核为 `APPROVED`，必须返修与非阻塞建议均为无，用户已确认 `CLOSED`；审核开始/结束七文件聚合 SHA-256 均为 `ed4779ee62adb58f09055138866ad8a78cd1e172c9383d840166d7f6da8fcae3`，scope 无漂移。七原语 TOF/TP/R_TRIG/F_TRIG/SR/RS/BLINK engineering adapter、十键默认 Registry、跨拍/OmitPolicy/双实例对照与 RISKS/ROADMAP 的 10/22 状态已收口，Git/GitHub 收尾已由 PR #22 完成。审核沙箱正式/全仓各有同 9 项面板端口权限假失败；协调器停止后在主机环境补跑协作 144/144、正式 tests 1299/1299、prototype_05 68/68、全仓 1367/1367，全部 `OK`。其余 12 个业务块 adapter、F2、参数装载、monitor/watchdog、HAL、真实 I/O 和现场证明继续排除。

---

## 1. 项目一句话

把 CODESYS SP16.1 软 PLC 复刻为 Python 原生软 PLC 平台（ST+CFC 双前端 → 语言无关可执行 IR → 扫描引擎），已迁移 14 业务块 + 8 原语作标准库，并建立了正式 L3 IR、静态校验、Store、实例布局、过程映像基础、显式顺序执行器核心、生产 OutputPolicy、外层故障安全扫描运行器、提交监督器、已审核关闭的 Python shadow mode 核心，以及已审核关闭并通过 PR #24 合并的 L2 Registry/**22 个 engineering adapter** 纵向链，目标是控制+AI 同平台一体化（分进程）。

## 2. 当前位置

- **阶段 1 / M1 Python headless MVP 功能闸门已通过并经 PR #34 合并**（2026-08-06）：`WP-20260804-072 CLOSED` 在正式 Claude 回审和 Codex 独立审核后收口 startup/readiness、Python 3.9 兼容和阶段 1 跨组件总验收；连同 `WP-20260805-075 CLOSED` 的工程支持已通过 [PR #34](https://github.com/yao501/PLC_to_Python/pull/34) 合并（merge `8840b2a…`，候选 head `3598c05…`，两者 Git tree 均为 `fd0e2817…`），`main == origin/main == HEAD == 8840b2a…`。既有定向复核 **71/71** 等测试事实原样保留、非合并后重跑。“阶段完成”仅指路线所定义的 Python 内部无界面引擎闸门；最终 ST/CFC 用户入口、CODESYS 导入/对拍、真实调度/多任务、HAL/I/O、硬件 watchdog、持久化与现场验证仍属后续阶段。
- **Claude 实施 Runbook 与启动器强制阅读已审核关闭并合并**（2026-07-30，`WP-20260730-052 CLOSED`，PR #32）：每个无跨进程记忆的 Claude CLI 首轮/返修都会先读取 Runbook，并由统一 prompt 内联零写入检查、允许命令、v2 精确字段、真实时间、失败关闭和 Git/GitHub 禁令；默认 `--max-turns 80`。该项是工程协作基建，不计入软 PLC 产品能力；独立审核后宿主正式 tests **1568/1568**、`prototype_05` **68/68**、全仓 **1636/1636**。
- **阶段 1 单任务运行栈纵向装配 + 手搭 TON 最小程序 E2E 已审核关闭并合并**（2026-07-30，`WP-20260730-050 CLOSED`；承接 `WP-20260730-049` 配额中断检查点，来源包原始 `BLOCKED` 记录保留、顶层已由后继收口为 `CLOSED`；已通过 PR #30 合并）：新增 `src/runtime/task_runtime.py::build_task_runtime` / `TaskRuntimeAssembly`（从 `src.runtime` 导出），把 `build_runtime → SafetyStateService → OutputPolicyService → CommitSupervisor → 默认 shadow CommitPort → ScanEngine → OuterScanRunner → SoftwareCycleMonitor` 连成**同一对象图**（不建第二套 Store/策略/监督/安全状态）；默认物理写关闭（shadow），唯一放开写仍是 `runner.set_write_enabled(True)`；`initial_safety=None` 用冻结冷启动失败关闭快照 `COLD_START_SAFETY`（强制 safe、不自动释放）。`tests/test_runtime_task_runtime.py` 23 条锁定默认 shadow 冷启动、手搭 TON（`Motor=TON.Q AND NOT Stop`、`cycle_ms=500`）逐拍对照 `TON.step`/Stop 抑制/`prev` 前移、显式退出 shadow 恰一次提交+回执、对象图身份、双实例隔离、装配失败原子性（driver 零调用/无 assembly）、scan/commit fault 分层、monitor 手工 exact-int 纳秒时钟一次性派发/不重放/业务不推进。Claude 宿主正式 tests **1560/1560**、`prototype_05` **68/68**、全仓 **1628/1628**、导入冒烟均通过；Codex Round 3 独立审核 `APPROVED`、必须返修和非阻塞建议均为无。上述现为主线已合并证据。`build_task_runtime` 仍是 Python 内部/测试装配入口，**非最终用户编程入口**；真实调度/多任务/startup inhibit 计时与释放/外部信号源/HAL/CFC 定序/CODESYS 对拍未实现，`RUNTIME-TASK-ASSEMBLY` 保持 in-progress，且不使 `RUNTIME-SAFETY-DEFAULT`/`RUNTIME-SHADOW-MODE`/`RUNTIME-WATCHDOG`/`RUNTIME-PARAM-VALIDATION` 转 resolved。
- **参数装载与启动校验首个纵向子范围已审核关闭**（2026-07-29，`WP-20260729-042 CLOSED`；承接 `WP-20260728-041` 的历史阻塞检查点）：公开 `build_runtime` 先汇总 IR/L2/构造覆盖/Store 初值/时间目录/startup inhibit 配置错误，再一次性失败或构建 Store/Executor；递归 user-FB、非法 `numeric_mode` 与 APCHSACCUM 直连非法构造值均有稳定失败关闭反证，两入口同源校验避免规则漂移。最后完整主机快照为正式 tests **1441/1441**、`prototype_05` **68/68**、全仓 **1509/1509**。这只关闭当前静态启动装配子范围；外部配置源、运行期动态值、持久化、monitor/watchdog、HAL/I/O 与 PLC/现场证明仍未完成。
- **L2 engineering adapter 22/22 目录已完成独立验收**（2026-07-28，`WP-20260728-040`，本包实施+自审全绿、待 Codex 独立审核收口）：在 `tests/test_runtime_descriptors.py` 与 `tests/test_runtime_executor.py` 新增集中式 22 键目录反证与可机器解析的 22 项行为覆盖矩阵（含要求 6 全部横切语义：required 缺失 fail-closed 且不半写、`use_default` 每拍回落而非 keep_previous、keep_previous 分离、跨拍推进、双实例隔离、组合子实例隔离、tuple/dict/attr 标量输出回收、失败时 `_stepped` 不推进/`_driven` 清空/输出 Store 不半写、同 Executor 授权块共享同一 `LicenseContext`），把既有代表性/七原语/五基础/七复杂逐拍对照连成完整目录证据。九组命令全绿：正式 tests **1383/1383**、`prototype_05` **68/68**、全仓 **1451/1451**。仅目录已验收；F2、参数装载、`monitor/watchdog`、HAL、真实 I/O、CODESYS 对拍与现场安全仍为独立未验证边界，`PLATFORM-EXEC-STORE-ATOMICITY-1` 局部 in-progress 不因目录验收转 resolved。**Git 事实**：`main == origin/main == HEAD == 72d32ea…`，PR #22/`da6ff139…` 只是 10/22 历史已合并节点，工作区含 `WP-026`～`039` 已审核未 Git/GitHub 收尾的累积改动加本包新增测试，**不干净、未合并**。上述 Python 证据不构成 PLC/CODESYS/HAL/I/O/现场安全一致性证明。
- **L2 registry 核心与代表性 adapter 纵向链已审核收口**（2026-07-24，最终承接包 WP-022 `APPROVED`、用户已确认 `CLOSED`）：已建立纯数据 `BlockSchema`、进程内 `RuntimeAdapter`、`(block_type, variant)` 唯一注册表、Loader 解析、Store pin 过程映像、Executor 库块调用，以及 TON/APCHSHLLIM/APCM 三个代表性 adapter。OmitPolicy 已锁定 `required` / `use_default` / `keep_previous` / `none_means_no_write`，其中 `_stepped` 仅在 adapter、全部 `VAR_IN_OUT` 与声明输出完整成功回收后推进，失败路径清空本拍 `_driven`。Claude 环境最终为新增反证 **1/1**、既有反证 **6/6**、L2/IR/Store/Executor **197/197**、安全运行时相关 **240/240**、正式 tests **1281/1281**、原型 **68/68**、全仓 **1349/1349**；Codex 审核沙箱正式/全仓各有同 9 项 scope 外面板用例因禁止绑定端口报环境错误，排除该模块后分别 **1137/1137**、**1205/1205** 通过。不同计数是环境与测试集合快照差异，不是功能矛盾。完整 14+8 描述符目录、其余 19 个 adapter、F2、真实 HAL/monitor、PLC/CODESYS 对拍与现场证明仍未完成；上述 Python 证据不构成现场发布授权。
- **最新 v2 自审与独立审核验证**（2026-07-23，WP-015 Round 3 `APPROVED`、用户已确认 `CLOSED`）：Claude 最终交接为定向 shadow/engine/runner/policy/supervisor **240/240 通过**、既有运行时 **166/166 通过**、正式 tests **1222/1222 通过**、0.5 原型 **68/68 通过**、全仓 **1290/1290 通过**。Codex 独立复核普通对象图底层提交能力不可达、零配置两拍 shadow、直接 `port.commit` 抑制及 shadow→实写首拍从 `safe_value=0` 限速到 5；开始/结束 scope 哈希均为 `f96d2a05…991bf`，`git diff --check` 通过。审核沙箱中正式/全仓各有同 9 个既有 HTTP 端口用例因禁止绑定而不能运行，其余 1213/1213、1281/1281 通过；随后在允许本地端口绑定的环境完整复跑 1222/1222、1290/1290 通过。WP-011 的 172/166/1172/68/1240、WP-008 的 144/274/1108/68/1176 等均为不同时点证据；增长来自后续 APCM 与 shadow 回归，属正常测试增长，不是矛盾。以上只证明当前 Python 实现和协作工具行为，不是目标 PLC/CODESYS、真实 HAL/驱动或真机安全一致性的证据。
- **WP-006 审核证据快照**（2026-07-16，Codex 对 `WP-20260716-006` Round 1 独立复跑）：定向 `tests.test_runtime_engine` = **28/28 通过**、`tests.test_runtime_executor` = **58/58 通过**、`tests.test_runtime_store` = **24/24 通过**、`tests.test_runtime_ir` = **56/56 通过**；当时正式 tests = **937/937 通过**、0.5 原型 = **68/68 通过**、全仓 = **1005/1005 通过**。当时之后的计数增长来自后续 WP-007/WP-008 运行时用例与协作基础设施回归；其中协作基础设施曾单独新增 1 项监听器就绪回归和 2 项项目内心跳回归。不同数字是不同时点的真实测试快照，历史证据保留原始计数，不回写冒充当时结果。以上 Python 测试均不证明与目标 PLC 语义一致。

- **阶段 0.5（语义基线修订）**，文档侧已完成**三轮**外部评审（ChatGPT5.5）修正；评审方判断"主体架构已站住，无需再大规模文档重构"。
- 规格版本：`IR_SPEC` **v2.2.4**（0.5 冻结基线 v2.2.2 + 阶段 1 `StackSlot.index` / 持久 Store 键两项工程约定写回）/ `ENGINE_SCAN_SPEC` **v2.2.2** / `COMPONENT_CONTRACT` **v2.1** / `TARGET_PROFILE` **v1.3** / `GOLDEN_TRACE_FORMAT` **v1.2.1**。`STAGE0_DESIGN.md` 已标历史文档，不再更新。
- **阶段 1 显式顺序执行器与五步扫描骨架已完成**：`WP-20260714-004` 三轮实现并在达到自动轮次上限后曾转 `BLOCKED`，其剩余问题由窄范围 `WP-20260714-005` 完整收口；WP-004/005/006 均已由用户确认 `CLOSED`。现已有正式 IR 值对象、装载期静态校验、声明制 Store 与隔离快照、PROGRAM/用户 FB 实例布局、原子输入锁存、输出待提交容器、显式顺序指令执行、TypedValue 求值栈、FUNCTION/用户 FB 调用帧、E/F1 数值边界以及可重复调用的确定性单拍扫描编排器。
- **生产 OutputPolicy 与安全状态快照已关闭**：`WP-20260716-007` 已把 `ENGINE_SCAN_SPEC §4` 的分原因策略、强制安全优先级、冷启动 `hold→safe_value`、正常路径限速、`last_effective` 状态与 IEC 非有限/越界值失败关闭落实为可直接注入 `ScanEngine` 的策略端口；Round 3 Codex 结论为 `APPROVED`、用户于 2026-07-20 确认 `CLOSED`。本包只消费原子安全状态，不生成 watchdog/scan-fault，也不实现 shadow、真实提交或 HAL。
- **外层安全扫描运行器已审核关闭**：`WP-20260720-008` Round 3 已获 Codex `APPROVED` 并由用户确认 `CLOSED`。`OuterScanRunner` 在正常路径继续复用同一 `ScanEngine` 与提交端口；提交前扫描异常或显式 watchdog 事件会绕过损坏 request，生成全通道安全映像并单次提交。`SafeImageTicket` 把 staging 与提交后策略历史确认拆成一次性两阶段事务，锁存/staging/commit/confirm 的失败均以保留原始与 fallback 异常的结构化信号上报。真实周期计时、后台线程、硬件 watchdog、shadow、真实 HAL、`last_physical_committed` 与提交故障锁存仍不在本包，须由后续工作包和真机验证承接。
- **提交监督恢复包已审核关闭**：`WP-20260721-009` Round 1～3 建立并逐步收敛驱动确认回执、`last_physical_committed`、逐通道 `commit_fault` / `channel_fault`、安全值重试、三条件显式复位、复位并发失败关闭与不可污染诊断快照；其 Round 4 中断历史保持 `BLOCKED`。后继 `WP-20260722-010` 已在 Round 2 收口普通异常 `__repr__` 与 fallback 类型名二次字符串化失败，Codex verdict=`APPROVED`、必须返修=无；未改变 `ENGINE_SCAN_SPEC v2.2.2 §4.1/§4.4` 语义，已由用户确认 `CLOSED`。
- **驱动回执类型信任边界已加固关闭**：`WP-20260722-011` 已将 WP-010 独立审核中识别、当时明确 scope 外的恶意整数子类风险独立收口。不可信驱动回执子类不再能在监督器置故障前触发重载运算；故障通道形成 `PartialCommitError`、计数与锁存升级，健康通道仍独立成功，Store 对业务内部 IEC 值的现有工程映射未改。Codex Round 1 `APPROVED`，已由用户确认 `CLOSED`。
- **Python shadow mode 核心已审核关闭**：`WP-20260722-012`、`WP-20260723-013` 与 `WP-20260723-014` 的中断历史据实保持 `BLOCKED`；其检查点由 `WP-20260723-015` 接续并在 Round 3 完整收口。零配置 `CommitPort(real_committer)` + `OuterScanRunner(...)` 默认形成可运行 write-disable 栈；物理写须显式 `legacy_unshadowed=True` 或经唯一受支持切换；gate/port/runner 的普通可达属性图不能取得可旁路的底层提交能力；shadow 正常、scan-fault、watchdog 均零物理写且诊断不冒充成功；shadow→实写先全通道边界重建，首拍从 `safe_value` 限速。用户已确认 WP-015 `CLOSED`。真实 HAL/可信反馈、实时 monitor、硬件 watchdog、趋势对拍、PLC/CODESYS 和现场安全证明仍排除并保持后续独立工作包。
- **0.5 可执行验证原型已完成并经两轮定向返修**（Fable5 实施，`prototype_05/`，一次性代码）：最小指令集 + TON 经描述符 + BOOL OutputPolicy + ST/CFC 双路径同指令列表跑 24 拍 + 5 个语义敏感案例。Codex 首轮 6 条（驱动异常提交隔离、绑定 actual 类型、OutputPolicy 校验、无 LPC 基准、纯整数 DIV/MOD、文档对齐）+ 二轮 2 条（Binding 表结构校验：重复 formal/非法 actual_kind/const 值类型；安全配置 NaN/Infinity/整数范围拒绝）均修复，每条有反证测试（`prototype_05/tests/test_review_rework.py`）。
- **下一步（边界分离）**：① 先完成 Fallback Lite 正式回审（当前本地未跟踪、哈希冻结的六文件候选须经后续 Claude 正式回审和 Codex 独立审核，不混入本次已合并里程碑）；② 再完成阶段 2 CFC 编译器内核正式回审；③ `.export` 自动模式顺序重建仍属阶段 5，算法未冻结前拒绝生成可执行 IR；④ 真实实时循环/调度、连续 deadline miss、外部参数源/优先级、HMI、RETAIN/PERSISTENT、F2、HAL/I/O、硬件 watchdog、CODESYS SP16.1 对拍与现场安全继续分别立项。

## 3. 文档权威地图（谁说了算）

| 主题 | 权威文件 |
|---|---|
| 阶段/里程碑/原型范围 | `docs/PLATFORM_ROADMAP.md` |
| IR 指令集/类型规则/POU 模型/lowering | `docs/IR_SPEC.md` |
| 一拍时序/OutputPolicy/输出两层状态/能力边界 | `docs/ENGINE_SCAN_SPEC.md` |
| 块描述符/注册表/省略语义 | `docs/COMPONENT_CONTRACT.md` |
| 一致性等级 E/F1/F2、数值语义基线 | `docs/TARGET_PROFILE.md` |
| 对拍轨迹格式/采集清单 | `docs/GOLDEN_TRACE_FORMAT.md` |
| 风险/待办登记簿（唯一） | `docs/RISKS.md` |
| 软 PLC 功能矩阵（功能点级状态索引，六轴分离；只引用不取代规格/风险） | `docs/SOFT_PLC_FUNCTION_MATRIX.md` |
| 编码规则 | `.cursor/rules/00a`、`04-platform-runtime.mdc` |
| AI 协作纪律（长期方针） | `CODEX_GUIDE.md`（本文件是短期快照，职责不混写） |

## 4. 决策索引（2026-07-12 冻结评审已裁决并经写回复核生效；细节看对应权威文件）

- D1 外挂描述符（块零改动）；D2 programs 列表顺序；**D3 载体分支（2026-07-12 裁决）**：PLCopen XML 保留显式 `executionOrderId`（首选载体）、.export 自动模式序号须重建（算法未冻结，未就绪拒绝生成可执行 IR）、新建图拓扑定序；D4/D5 数值双模式 E/F1/F2；D-AI 控制与 AI 分进程。
- **OutputPolicy 物理基准与复位（2026-07-12 裁决，项目工程约定）**：边界首拍基准 = 可信反馈优先，否则 `safe_value`；`last_physical_committed` 不冒充反馈；`channel_fault` 锁存、显式复位（`ENGINE_SCAN_SPEC` v2.2.2 §4.1/§4.4）。
- IR：全类型化指令 + TypedValue 栈 + 加载期类型验证；POU 定义与实例分离（实例装载期展开，调用不建实例）；PROGRAM/用户 FB 持久 Store 键 = `<实例全路径>.<变量名>`（项目工程约定，非 CODESYS/IEC 官方命名语义）。
- F1 = 边界量化（F1-expr/F1-boundary 两子行为），**不承诺** bit-exact；F2 = 位级候选须真机证明；模式禁止热切换。
- OutputPolicy 按故障原因分策略，safety/scan_fault/watchdog 强制 safe；last_effective / last_physical_committed 两层。
- 注册表键 `(block_type, variant)`；Pin 省略语义四值枚举。

## 5. 未决/阻塞项（详见 RISKS.md 三-A）

- ⬜ 用户补（余一项）：是否要 F2。~~CPU/OS、CFC 顺序模式、Patch 级别~~ 均已由导入试验两份样本补齐（`TARGET_PROFILE` v1.3，标"样本工程实测"）。
- 🟨 真机黄金轨迹实采：外部阻塞，需 SP16.1 环境（格式已就绪 v1.2.1，含 #7 整数溢出裁决用例）。
- 🟥 `int_intermediate_policy`、CFC 反馈边映射、REAL 中间精度：均为待真机裁决假设。
- ✅ 0.5 原型两轮返修完成（2026-07-05，55/55 全绿——历史子集计数，最新记录 68/68 见 §2），**Codex 代码复核已通过**（PLC 一致性未证明，属阶段 6 范围）。~~评审裁决遗留：`PLATFORM-OUTPUT-BASELINE-1`~~✅ 已于 2026-07-12 冻结评审通过并写回 `ENGINE_SCAN_SPEC` v2.2.2（属项目工程约定、非 CODESYS 官方语义，阶段 7 HAL 实现与现场验证未完成）；有符号回绕作待真机假设保留。
- ✅ 真实 CODESYS 导出最小导入试验完成（2026-07-09，`PLATFORM-IMPORT-TRIAL-1` done，见 `prototype_05/import_trial/FINDINGS.md`）。
- ⏸ `PLATFORM-CFC-AUTOORDER-1`（deferred/mitigated，2026-07-12）：D3 已裁决为载体分支并写回；.export 自动模式重建算法延后阶段 5，未就绪时导入器拒绝生成可执行 IR。
- 🟨 `PLATFORM-CFC-FEEDBACK-MAP-1`：首份真实反馈环样本已到（入环元素序号最小、回接输入读上一拍，与 `LOAD_PREV` 假设方向一致）；PLCopen 载体无显式反馈标记，`IsFeedbackStart` 落点对照（同工程 .export）为可选项，不阻塞冻结。
- ✅ Git 基线与仓库清洁：Codex 已于 2026-07-13 审查暂存范围并实际运行 68/690/758 三组测试；基线提交 `63e79fc` 已通过 PR #1 合并至 `main`（merge commit `3bff318`），基线后治理状态同步通过 PR #2 落入 `main`；此前已被跟踪的两个 `.DS_Store` 已在独立清洁任务中停止跟踪，本机文件继续由 `.gitignore` 排除。

## 6. 新会话启动方法（给用户）

开新会话时用统一开场（与 `CODEX_GUIDE.md §5` 一致，替换任务名即可）：

> 先读 `CODEX_GUIDE.md` 和 `docs/PROJECT_STATE.md`，再只读本任务相关的权威文件。当前任务：〈任务，如"实现 0.5 可执行验证原型"〉。

- **一个会话 = 一个工作包**（如"写原型"、"写 ST 词法"、"评审反馈修订"）。工作包完成且状态有实质变化时更新本文件，再关闭会话；别在同一会话里连做多个阶段。
- 评审往返（贴外部反馈→修文档）单独开会话。
- 会话内明显变慢时，先把当前进度写进本文件再开新会话——只要状态与规格及时更新，信息损失可以很低（不是零：对话中的推理过程与未落档的备选方案会丢，重要结论务必落档）。
